# react-leaflet@4 declares react@^18 as peer; we run React 19. Runtime is fine
# (react-leaflet treats react as a peer it never re-evaluates), but npm errors
# without this flag. Revisit when react-leaflet ships a v5 stable that targets 18+/19+.
legacy-peer-deps=true
